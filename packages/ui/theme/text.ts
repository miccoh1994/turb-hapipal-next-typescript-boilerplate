const Text = {
  // Styles for the base style
  baseStyle: {
    color: 'inherit',
    width: 'inherit',
    textAlign: 'left',
    maxWidth: '500px'
  },
  // Styles for the visual style variations
  variants: {},
  // The default `size` or `variant` values
  defaultProps: {
    color: 'white'
  }
};

export default Text;
